---
apiVersion: v1
kind: Service
metadata:
  name: gracedb-test
  namespace: gracedb-test
spec:
  #type: NodePort
  ports:
  - name: http
    port: 80
    targetPort: 80
  - name: p1
    port: 8080
    targetPort: 8080
  selector:
    app: gracedb-test
